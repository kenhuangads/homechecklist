# 雲端儲存設定（Google 試算表）— 只需做一次，約 5 分鐘

家人打開網址就能用、不用登入；所有修改會存進 **你的 Google 試算表**（含完整修改紀錄與快照）。
這個版本只要求「目前這份試算表」的權限，不會碰雲端硬碟其他檔案，授權時不會再出現「This app is blocked」。

## 步驟
1. 用你的 Google 帳號開一個**新的 Google 試算表**，命名「南投老家翻修清單」。
2. 上方選單 **擴充功能 → Apps Script**。把編輯器裡預設的內容全部刪掉，貼上 `apps-script/Code.gs` 的全部內容，按 **儲存**（磁片圖示）。
   - 第一行 `const FAMILY_CODE = '2026';` 可改成你想要的家庭代碼（家人第一次修改時輸入一次；留空 `''` 代表不檢查）。
3. （建議）左側 **專案設定（齒輪）** → 勾選「在編輯器中顯示 appsscript.json 資訊清單檔案」→ 回到編輯器打開 `appsscript.json`，整個換成 `apps-script/appsscript.json` 的內容並儲存。這會把權限鎖定在「目前這份試算表」。
4. 在編輯器上方的函式下拉選 **selfTest** → 按 **執行**。這時會跳授權視窗：選你的帳號 → 若出現「Google 尚未驗證這個應用程式」→ 點 **進階** → **前往 …（不安全）** → **允許**。執行成功後試算表會多出 `history` 等分頁。
5. 右上角 **部署 → 新增部署作業**：左上「選取類型」齒輪 → **網頁應用程式**；執行身分 **我**；誰可以存取 **所有人** → **部署**。
6. 複製 **網頁應用程式網址**（結尾 `/exec`），貼給 Claude，或自己填進 `docs/config.json` 的 `"apiUrl"` 後 push；幾分鐘後網站就會連上雲端。
7. 驗證：瀏覽器打開 `<網址>?action=ping`，看到 `{"ok":true,...}` 就成功。

## 如果授權還是被擋
- 先確認第 3 步的 `appsscript.json` 有換成只含 `spreadsheets.currentonly` 的版本（舊版用到雲端硬碟權限才會被封鎖）。
- 若帳號有開啟 Google「進階保護計畫」，未驗證的應用程式會被一律封鎖；可以改用另一個 Google 帳號建立這份試算表。

## 之後
- 試算表分頁：`history`（誰、哪台裝置、幾點、改了什麼）、`summary`（各品項目前狀態）、`snapshots`（每次儲存的快照，最近 120 份，可在網站「紀錄 → 雲端快照」整份回復）、`state`（壓縮後的目前資料，請勿手動編輯）、`meta`。
- 若日後更新了 `Code.gs`：**部署 → 管理部署作業 → 編輯（鉛筆）→ 版本選「新版本」→ 部署**，網址不會變。
