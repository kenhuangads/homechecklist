# 雲端儲存設定（Google 試算表）— 只需做一次，約 5 分鐘

家人打開網址就能用、不用登入；所有修改會存到 **你的 Google 試算表／雲端硬碟**，並保留完整修改紀錄與快照。

## 步驟
1. 用你的 Google 帳號開一個**新的 Google 試算表**，命名「南投老家翻修清單」。
2. 上方選單 **擴充功能 → Apps Script**。把編輯器裡預設的內容全部刪掉，貼上 `apps-script/Code.gs` 的全部內容。
3. 第一行 `const FAMILY_CODE = '2026';` 可以改成你想要的家庭代碼（家人第一次修改時要輸入一次，防止陌生人亂改；留空 `''` 代表不檢查）。按 **儲存**（磁片圖示）。
4. 右上角 **部署 → 新增部署作業**：
   - 左上「選取類型」齒輪 → **網頁應用程式**
   - 執行身分：**我**
   - 誰可以存取：**所有人**
   - 按 **部署** → 會要求授權：選你的帳號 → 若出現「Google 尚未驗證這個應用程式」→ 點 **進階** → **前往 … (不安全)** → **允許**
5. 複製畫面上的 **網頁應用程式網址**（結尾是 `/exec`）。
6. 把這個網址貼給 Claude，或自己填進 `docs/config.json` 的 `"apiUrl"` 後 push；幾分鐘後網站就會連上雲端。
7. 驗證：瀏覽器打開 `<網址>?action=ping`，看到 `{"ok":true,...}` 就成功。

## 之後
- 試算表會自動多出三個分頁：`history`（誰、哪台裝置、幾點、改了什麼）、`summary`（各品項目前狀態）、`meta`。
- 雲端硬碟會出現資料夾 `homechecklist-data`：`state.json`（目前資料）與 `snapshots/`（每次儲存的快照，保留最近 200 份）。
- 若日後更新了 `Code.gs`，要 **部署 → 管理部署作業 → 編輯（鉛筆）→ 版本選「新版本」→ 部署**，網址不會變。
